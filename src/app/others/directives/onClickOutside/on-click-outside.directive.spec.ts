import { OnClickOutsideDirective } from './on-click-outside.directive';

describe('OnClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new OnClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
